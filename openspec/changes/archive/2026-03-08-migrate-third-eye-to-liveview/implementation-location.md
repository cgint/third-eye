# Implementation location

The Phoenix LiveView rewrite is implemented in the sister repository:

- `../third-eye-liveview`

This repo (`../third-eye`) remains the legacy SvelteKit implementation and is used as:
- UX reference (behavioral baseline)
- source for copying static assets as-is (manifest/icons) when needed

OpenSpec task progress is tracked primarily in `../third-eye-liveview/openspec/changes/migrate-third-eye-to-liveview/tasks.md`.
